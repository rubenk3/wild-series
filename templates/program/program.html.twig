{% extends "base.html.twig" %}

{% block body %}
    {{ form(form) }}
    {{ form_start(form) }}

    {{ form_widget(form) }}

    <button type="submit">Add program</button>

    {{ form_end(form) }}
{% endblock %}